// stb_vorbis's implementation, compiled exactly once.
//
// stb_vorbis v1.22, MIT OR public domain (vcpkg port "stb"). Third-party code,
// so this file is built with warnings off (see CMakeLists.txt) and contains
// nothing of ours. See docs/provenance/son.md.
#include <stb_vorbis.c>
